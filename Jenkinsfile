pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }

        stage('Dos') {
            steps {
                sh 'ls -l /'
            }
        }

     }
}
